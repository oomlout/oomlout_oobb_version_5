# withering_rise_logo

Rounded-square RISE athlete-village logo badge. The source raster artwork is
traced into a monochrome, path-only SVG and subtracted from the top face for
engraving or colour inlay.

This component supports the same variables as `withering_bvu_logo`.
