# withering_10north_logo

Rounded-square 10 North logo badge. The supplied SVG artwork is subtracted
from the top face for engraving or colour inlay. The source logo's white
background is omitted while its vector artwork is otherwise unchanged.

This component supports the same variables as `withering_bvu_logo`.
